description "accumulo tracer"

start on stopped rc RUNLEVEL=[2345]
respawn
setuid accumulo
chdir /var/lib/accumulo

exec /usr/bin/accumulo tracer --address {{ ansible_eth1['ipv4']['address'] }} >>/var/log/accumulo/logs/tracer.out 2>>/var/log/accumulo/logs/tracer.err
